# Runtime Configuration (APP_*)
APP_ENV=dev
APP_HOST=127.0.0.1
APP_PORT=8000
APP_LOG_LEVEL=info

# Metadata (FLOWBIZ_*)
FLOWBIZ_SERVICE_NAME=flowbiz-client-ai-builder
FLOWBIZ_VERSION=0.1.0
FLOWBIZ_BUILD_SHA=local

# Core Integration (CORE_*)
CORE_SERVICE_MODE=mock
# CORE_SERVICE_URL=https://core.example.com  # Uncomment and set when using mode=real
